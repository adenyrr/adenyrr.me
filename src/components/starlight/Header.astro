---
/**
 * Custom Starlight Header — renders the SAME site-wide header
 * as the rest of the site (config-driven, ThemeToggle, mobile menu).
 */
import type { Props } from '@astrojs/starlight/props';
import Header from '../Header.astro';
---

<Header />
